import { getData } from '../src/client/js/dataGetter'


describe("Requesting data from external API with the created parameters", () => {
    
    test("Generates request URL, fetches data and populates DOM elements", () => {
     
           expect(getData).toBeDefined();
})});